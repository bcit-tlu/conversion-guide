apiVersion: v1
kind: Service
metadata:
  name: {{ include "conversion-guide.fullname" . }}
  labels:
    {{- include "conversion-guide.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "conversion-guide.selectorLabels" . | nindent 4 }}
